import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    //type: 'dark',
    primary: {
      main: '#757f49',
    },
    secondary: {
      main: '#795548',
    },
    background: {
      default: '#e0e0e0',
      paper: '#9e9e9e',
    },
  },
})